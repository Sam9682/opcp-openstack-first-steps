services:
  skillhub:
    image: nginx:alpine
    container_name: skillhub
    ports:
      - "8080:80"
    volumes:
      - ./skillhub:/usr/share/nginx/html:ro
      - .running_skillhub/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
